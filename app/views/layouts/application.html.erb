<!DOCTYPE html>
<html>
	<head>
		<title>Orcamento</title>
		<%= stylesheet_link_tag :all %>
		<%= javascript_include_tag :defaults %>
		<%= csrf_meta_tag %>
	</head>
	<body>

		<div id="view">
			<%= yield %>
		</div>
	</body>
</html>
